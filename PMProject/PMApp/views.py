from distutils.command.install import value

from django.shortcuts import render

# Create your views here.
from PMProject.PMApp.models import Task

'''
def List():
    Task.objects.all()
    for i in Task:
      lst=[i]
      c={i}
    return render('base.html',{c})'''